# 家长登录管理

## 使用方式

1. 页脚点击 **「家长入口」**，输入密码后登录。
2. 登录后可：
   - **家庭相册**：上传照片、删除照片（localStorage 持久化）。
   - **每日计划**：添加 / 编辑 / 删除计划项，孩子可勾选完成（localStorage 持久化）。
3. 点击 **「退出管理」** 退出家长模式。

## 默认密码

- 当前默认密码：**`parent2024`**
- 修改方式：编辑 `src/contexts/ParentAuthContext.tsx` 中的 `DEFAULT_PASSWORD`。后续可改为环境变量或后台配置。

## 后续可扩展

- 修改学习基地、体育中心、娱乐星球等模块内容。
- 密码改为从后台获取或支持多账号。
- 相册 / 计划数据存到服务器而非 localStorage。
